cmake_minimum_required(VERSION 3.10)
project(sysmgr)
aux_source_directory(. DIR_SRCS)
add_executable(sysmgr ${DIR_SRCS})
include_directories(${MINIGUI_INCLUDE_DIRS} ${MGEFF_INCLUDE_DIRS})
target_link_libraries(sysmgr ${MINIGUI_LIBRARIES} ${MGEFF_LIBRARIES})
