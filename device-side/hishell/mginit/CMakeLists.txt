cmake_minimum_required(VERSION 3.10)
project(mginit)
aux_source_directory(. DIR_SRCS)
add_executable(mginit ${DIR_SRCS})
include_directories(${GLIB_INCLUDE_DIRS} ${MINIGUI_INCLUDE_DIRS} )
target_link_libraries(mginit ${MINIGUI_LIBRARIES} ${GLIB_LIBRARIES})
